# SC2 Commander Bot — Continuous Improvement List

> Generated by automated test/inspect/fix loop on `claude/stoic-shannon-ejumZ`.
> Each item links a concrete observed issue to a fix delta. Items marked
> "DONE" are committed in this branch; pending items roll into the next round.

## Round 1 — Test Infrastructure Hardening

| # | Issue | File | Severity | Status |
|---|-------|------|----------|--------|
| 1 | `from sc2.ids.unit_typeid import UnitTypeId` at module top-level halts entire `pytest tests/` run with collection ImportError when `burnysc2` is not installed. Every other sc2-touching test in the repo uses a `try/except ImportError` + `pytest.skip(..., allow_module_level=True)` pattern; this file is the lone outlier. | `tests/test_queen_transfusion.py` | HIGH (breaks CI) | DONE |
| 2 | `TestFuzzTesting::test_import` looks for class names `FuzzTarget` / `FuzzConfig` that do not exist in `fuzz_testing/sc2_fuzzer.py`. The actual public surface is `FuzzInput`, `CrashReport`, `Mutator`, `CoverageTracker`, `CrashAnalyzer`, `SC2Fuzzer`. Test is silently skipping. | `tests/test_p606_infra.py` | MED | DONE |
| 3 | `TestContractTesting::test_import` looks for `ContractViolation` / `ContractType`. Actual classes are `ContractStatus`, `HttpMethod`, `SchemaType`, `InteractionRequest`, `Contract`, etc. Silently skipping. | `tests/test_p606_infra.py` | MED | DONE |
| 4 | `TestSBOMManager::test_import` looks for `PackageType` / `SBOMFormat`. Actual classes are `Package`, `Vulnerability`, `SBOMDocument`, `DependencyScanner`, `SBOMGenerator`. Silently skipping. | `tests/test_p606_infra.py` | MED | DONE |
| 5 | `TestPerformanceProfiler::test_import` looks for `ProfileMetric` / `ProfilingConfig`. Actual classes are `Timer`, `MemorySnapshot`, `MemoryTracker`, `CPUProfiler`, `FrameBudget`, `FrameAnalyzer`, `FlameGraphGenerator`, `SC2Profiler`. Silently skipping. | `tests/test_p606_infra.py` | MED | DONE |

After Round 1: collection runs without ImportError, and 4 previously
"skipped — not importable" tests now exercise real symbols.

## Round 2 — Code Quality & Inspection (open queue)

Pending items get filled as the loop progresses. Each round records what
was found, what was fixed, and any leftovers escalated for the next round.
