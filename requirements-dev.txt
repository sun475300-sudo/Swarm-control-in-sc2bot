# Development / Testing Dependencies
# Install with: pip install -r requirements-dev.txt

# Testing
pytest>=7.0.0
pytest-asyncio>=0.23.0
pytest-timeout>=2.1.0
pytest-mock>=3.10.0

# Test runtime helpers used by the test suite
# numpy is imported by P606+ modules (pettingzoo_env, world_model, etc.)
numpy>=1.24
# PyYAML is required by config_loader and crypto/security tests
PyYAML>=6.0

# Code quality (matches sc2bot-ci.yml versions)
black==26.3.1
isort>=5.12.0
flake8>=6.0.0
