# ═══════════════════════════════════════════════
# Development & Test Dependencies
# Install with: pip install -r requirements-dev.txt
# ═══════════════════════════════════════════════

# Test framework
pytest>=7.0.0
pytest-asyncio>=0.21.0
pytest-cov>=4.0.0
pytest-timeout>=2.1.0

# Lint / format / type-check (mirrors CI in .github/workflows/sc2bot-ci.yml)
black>=24.0.0
isort>=5.12.0
flake8>=6.0.0
mypy>=1.5.0
bandit>=1.7.0
