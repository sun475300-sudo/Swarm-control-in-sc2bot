# ═══════════════════════════════════════════════
# Dev / Test Dependencies
# ═══════════════════════════════════════════════
# Install with: pip install -r requirements-dev.txt
# These are not required at runtime, only for running the test suite,
# linters, and type checks.

-r requirements.txt

# Test framework
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0
pytest-timeout>=2.2.0

# Linting / formatting
black>=26.3.1
isort>=5.13.0
flake8>=7.0.0
mypy>=1.8.0
bandit>=1.7.5
