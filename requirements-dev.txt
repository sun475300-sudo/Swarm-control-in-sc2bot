# Developer + CI dependencies (test, lint, type-check).
# Runtime deps live in requirements.txt; install both for local dev:
#   pip install -r requirements.txt -r requirements-dev.txt

# ─── Test framework ────────────────────────────────────────────────
# pytest-asyncio is REQUIRED — pytest.ini sets `asyncio_mode = auto`
# and most managers (combat, economy, expansion, intel, …) have async
# test bodies. Without this plugin the suite reports 80+ false fails.
pytest>=9.0.0
pytest-asyncio>=1.0.0
pytest-timeout>=2.4.0
pytest-cov>=5.0.0

# ─── Lint / format / static analysis ───────────────────────────────
ruff>=0.6.0
flake8>=7.0.0
black>=24.0.0
isort>=5.13.0
mypy>=1.10.0
