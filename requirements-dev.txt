# ═══════════════════════════════════════════════
# Development & Test Dependencies
# ═══════════════════════════════════════════════
# Use:  pip install -r requirements-dev.txt
# (does not include the runtime deps in requirements.txt)

pytest>=8.0
pytest-asyncio>=0.23
pytest-timeout>=2.3
pytest-cov>=5.0

# Lint / format
flake8>=6.0
black>=24.0
isort>=5.12

# Static analysis
mypy>=1.8
bandit>=1.7
