# SC2 Bot Core Dependencies (lightweight)
# Install with: pip install -r requirements-sc2bot.txt -r requirements-dev.txt
#
# Scoped to what wicked_zerg_challenger/ and tests/ actually need to import
# and run. The root requirements.txt pulls in unrelated ecosystems (crypto
# trading, Discord, Google APIs, AWS, TTS, torch, ...) whose combined
# constraints make pip's resolver back track for many minutes (observed
# multi-minute hangs / CI "resolution-too-deep" failures). CI jobs that only
# need to run the SC2 bot test suite should install this file instead.
burnysc2>=5.0.0
numpy>=1.20.0
psutil>=5.8.0
s2clientprotocol>=4.19.0.0
sc2reader>=0.8.0
pyyaml>=6.0
