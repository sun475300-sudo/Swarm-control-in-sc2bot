# Marker so that `scripts` resolves as a regular package (not a namespace
# package). Without this, pytest occasionally merges this directory with
# wicked_zerg_challenger/scripts/ and shadows ladder_tracker / meta_adapter.
