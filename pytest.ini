[pytest]
# pytest configuration for Wicked Zerg Challenger

# Test discovery patterns
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Test paths
testpaths = tests

# Output options
addopts =
    -v
    --strict-markers
    --tb=short
    --disable-warnings
    --color=yes

# Markers
markers =
    unit: Unit tests for individual components
    integration: Integration tests for multiple components
    performance: Performance and benchmark tests
    asyncio: Async test functions
    slow: Tests that take more than 1 second

# Coverage options (if pytest-cov is installed)
# Uncomment to enable coverage reporting
# addopts = --cov=wicked_zerg_challenger --cov-report=html --cov-report=term

# Asyncio mode
asyncio_mode = auto

# Timeout for tests (seconds). Requires `pytest-timeout` to be installed;
# otherwise pytest logs "Unknown config option: timeout" but the run is
# still safe — the option is no-op'd. Leaving it here so CI installs
# that pull pytest-timeout via requirements-dev get the protection.
timeout = 60

# Minimum Python version
minversion = 3.8
