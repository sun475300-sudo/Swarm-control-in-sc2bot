# SC2 Commander Bot — Improvement Backlog

Generated by automated test/inspection cycle. Each round commits and pushes.

## Round 1 — Complete (1160 passed, 14 skipped, 0 failed)

### P0 Test/CI breakage
- [x] **R1.1** `wicked_zerg_challenger/blackboard.py` only exports `GameStateBlackboard`, but `wicked_zerg_bot_pro_impl.py:31` and `production_controller.py:30` and `tests/test_blackboard.py:20` import `Blackboard`. Added `Blackboard = GameStateBlackboard` alias.
- [x] **R1.2** `tests/test_queen_transfusion.py` imports `sc2.ids.unit_typeid` at top level; switched to `pytest.importorskip("sc2.ids.unit_typeid")` so collection no longer aborts.
- [x] **R1.3** `production_controller.py:30` reads `from blackboard import Blackboard as GameStateBlackboard` — flipped. Now imports `GameStateBlackboard` directly.
- [x] **R1.bonus1** `should_expand()` referenced `self.resources.is_supply_block` — typo (no `d`). Test surfaced an `AttributeError`. Fixed to `is_supply_blocked`.
- [x] **R1.bonus2** `should_expand()` had no minerals gate, so the bot would commit to a Hatchery with insufficient resources. Added a 300-mineral threshold (cost of Hatchery).
- [x] **R1.bonus3** `scripts/` and `wicked_zerg_challenger/scripts/` collided as namespace packages, hiding `ladder_tracker` / `meta_adapter`. Added `scripts/__init__.py` to make root a regular package.

### P1 Hot-path performance / correctness
- [ ] **R1.4** `economy_manager.py` does `from blackboard import …` inside on_step methods (5 sites). Hoist to module-level conditional import.
- [ ] **R1.5** `wicked_zerg_bot_pro_impl.py` has `E402 module level import not at top of file` (10+ sites). Move imports above runtime code.
- [ ] **R1.6** 223 broad `except Exception:` handlers, many silenced with `pass`. At minimum log at debug to enable post-mortem.

### P2 Code quality
- [ ] **R1.7** Many E501 (line too long) in `blackboard.py` and `wicked_zerg_bot_pro_impl.py`.
- [ ] **R1.8** `monitoring/mobile_app_android` Kotlin TODOs about hard-coded gist/pastebin URLs and missing file IO.

## Round 2 — Complete (1163 passed, 14 skipped, 0 failed)

### Real bugs fixed
- [x] **R2.1** `Dict[str, any]` → `Dict[str, Any]` in
  `economy/queen_transfusion_manager.py:233` and
  `advanced_micro_controller_v3.py:1405`. The lowercase `any` is the builtin
  function; the annotation was meaningless to type checkers.
- [x] **R2.2** Memory leak in `QueenTransfusionManager._queen_last_cast` —
  dead queen tags accumulated forever. Fixed by pruning entries against the
  live queen roster at the start of `execute_transfusions`.
- [x] **R2.3** Same memory-leak class in `QueenManager.last_transfuse_time` /
  `last_creep_time` / `assigned_queen_tags` / `dedicated_creep_queens`.
  Added `_prune_dead_queen_state()` invoked every 100 iterations from
  `on_step()`.
- [x] **R2.4** New regression tests:
  `test_dead_queen_cooldown_pruned`, `test_should_not_expand_supply_blocked`,
  `test_should_expand_at_exact_minerals_threshold`.

## Round 3 — Complete (1165 passed, 14 skipped, 0 failed)

### Real bugs fixed
- [x] **R3.1** `combat_manager.py:3483` defined `non_combat_names` set but
  never used it. The "scout-swarm = threat" logic was actually triggering on
  ANY 3 nearby enemies — including 3 of our own drones near the mineral
  line if they were filtered as enemies via mock, or 2 drones + 1 worker.
  Now it only fires on 3+ scout-class non-combat units.
- [x] **R3.2** `combat_manager.py:_find_harass_target` had two definitions —
  the first (line 2809) was dead code shadowed by the smarter version at
  ~5005. Removed the dead first copy (pyflakes F811) and added a comment
  to prevent re-introduction.
- [x] **R3.3** New regression tests
  `test_is_base_under_attack_only_drones_is_safe` and
  `test_is_base_under_attack_three_overlords_triggers` to lock in the
  scout-vs-worker distinction.

## Round 4 — Complete (1165 passed, 14 skipped, 0 failed)

### Hygiene + small bugs fixed
- [x] **R4.1** `production_controller.py`: `best_unit` was tracked alongside
  `best_uid` but never logged or used. Now emits a debug log when training
  the most-deficient unit, and the `except Exception` swallow now also logs
  at debug instead of silently passing.
- [x] **R4.2** `production_controller.py`: dropped dead `game_time` variable
  in `_train_overlords` — the buffer logic is purely supply-driven.
- [x] **R4.3** `economy/queen_inject_optimizer.py`: dropped unused
  `actual_time = self.inject_cooldowns.get(...)` lookup that did no work.
- [x] **R4.4** `early_defense_system.py`: 3 `f"…"` log calls had no
  placeholders; demoted to plain strings (pyflakes F541).
- [x] **R4.5** `scouting_system.py`: dropped unused `Iterable` import.

### Investigated (decided not to change yet)
- `combat/micro_combat.py:1151` `ally_count` unused. The surround-angle
  formula uses `(zergling.tag % 8)` which intentionally distributes across
  8 fixed positions regardless of ally count. Behavioral change risk too
  high without combat-balance test coverage.
- `combat/harassment_coordinator.py:699` unused `distance`. Comment
  promises "safe distance maintenance" but the code never gates by it.
  Adding a safe-distance gate would change combat behavior — needs a
  designed change, not a refactor.

## Round 5 — Complete (1169 passed, 14 skipped, 0 failed)

### Real bugs fixed
- [x] **R5.1** **Critical**: `OpponentModeling.on_step` was defined twice
  (line 341 and 765). The second was a near-empty placeholder that just
  called `_detect_early_signals` — it shadowed the rich version that
  performs strategy prediction, build-order tracking, timing-attack
  detection, tech-progression tracking, AND blackboard sync. The bot's
  whole opponent-modeling pipeline was effectively a no-op for the rest
  of the game. Removed the placeholder (pyflakes F811).
- [x] **R5.2** `proxy_detector.py:49` computed `distance_to_base` but
  threw it away. Now uses it to boost confidence to 0.95 when the
  candidate proxy is more than 60 tiles from the enemy base (definitely
  midfield), keeping 0.8 otherwise.
- [x] **R5.3** New regression tests:
  - `TestOnStepIsRichVersion::test_on_step_writes_prediction_keys_to_blackboard`
    locks in that the surviving `on_step` actually publishes
    `predicted_strategy` / `prediction_confidence` / `observed_signals`
    to the blackboard.
  - `tests/test_proxy_detector.py` (new file, 3 tests) locks the
    distance-based confidence boost.

## Round 6 — Complete (1169 passed, 14 skipped, 0 failed)

### Dead-code shadowing surfaced
- [x] **R6.1** `economy_manager.py:1681`
  `_prevent_resource_banking` was shadowed by another definition at
  ~line 3258. The first targeted **defensive** spending (extra queens
  + spines + spores after 3 bases); the second targets **economic**
  spending (macro hatcheries, gas-worker reduction). Renamed the dead
  first to `_prevent_resource_banking_legacy_defense` and flagged it
  for human review — both strategies have value.
- [x] **R6.2** `economy_manager.py:3401` `_reduce_gas_workers` was
  shadowed by a smarter gas-banking-aware version at ~line 4091. Renamed
  the dead first to `_reduce_gas_workers_legacy_v1`.
- [x] **R6.3** `local_training/production_resilience.py:1467`
  `build_terran_counters` was shadowed by a TechCoordinator-aware
  version at ~line 1977. Renamed the dead first to
  `build_terran_counters_legacy_v1`.

### Decisions / follow-ups for human review
- **R6.followup1** (P1) The bot is **missing defensive resource-spending**
  (queens/spines/spores) because the more sophisticated economic
  variant of `_prevent_resource_banking` won the shadowing race. A
  human should decide whether to (a) merge the two strategies into a
  single method, (b) call both from `on_step`, or (c) leave the
  defensive variant unwired.

## Round 7 — Complete (1169 passed, 14 skipped, 0 failed)

### Real bugs fixed
- [x] **R7.1** `local_training/production_resilience.py:1264-1266` —
  diagnostic logging asymmetry. The bot computed `can_afford_zergling`,
  `can_afford_roach`, `can_afford_hydralisk`, then only emitted a
  warning when zerglings should be produced. Roach/Hydralisk warnings
  were silently dropped. Added the missing two warnings — operators
  now see "Should produce Roaches but not producing!" /
  "Should produce Hydralisks but not producing!" diagnostics.
- [x] **R7.2** Removed dead `bases` and `pending_hatch` locals at
  `_produce_army_unit` line 530 — the comment explicitly says we no
  longer gate on bases here, but the variables were still being
  computed.
- [x] **R7.3** Removed dead `roach_count` / `hydra_count` /
  `mutalisk_count` locals at line 790 — only the zergling cap is
  enforced; the others have no cap (logged in backlog as a possible
  follow-up).

### Follow-ups for human review
- **R7.followup1** (P2) Roach / Hydralisk / Mutalisk lack a soft cap
  similar to `_get_zergling_cap`. Currently the bot can produce
  unlimited counts of these. May or may not be desired — depends on
  composition strategy.

## Round 8+ — Planned
- E402 cleanup in `wicked_zerg_bot_pro_impl.py` — risky because reordering
  imports can hit circular-import problems. Need to test boot path.
- Tighten the 223 broad `except Exception:` handlers — at minimum add debug
  logging.
- Audit how `intel_manager` integrates with `combat_manager`.
- Profile `on_step()` cost by manager.
