# SC2 Commander Bot — Improvement Backlog

Generated by automated test/inspection cycle. Each round commits and pushes.

## Round 1 — Complete (1160 passed, 14 skipped, 0 failed)

### P0 Test/CI breakage
- [x] **R1.1** `wicked_zerg_challenger/blackboard.py` only exports `GameStateBlackboard`, but `wicked_zerg_bot_pro_impl.py:31` and `production_controller.py:30` and `tests/test_blackboard.py:20` import `Blackboard`. Added `Blackboard = GameStateBlackboard` alias.
- [x] **R1.2** `tests/test_queen_transfusion.py` imports `sc2.ids.unit_typeid` at top level; switched to `pytest.importorskip("sc2.ids.unit_typeid")` so collection no longer aborts.
- [x] **R1.3** `production_controller.py:30` reads `from blackboard import Blackboard as GameStateBlackboard` — flipped. Now imports `GameStateBlackboard` directly.
- [x] **R1.bonus1** `should_expand()` referenced `self.resources.is_supply_block` — typo (no `d`). Test surfaced an `AttributeError`. Fixed to `is_supply_blocked`.
- [x] **R1.bonus2** `should_expand()` had no minerals gate, so the bot would commit to a Hatchery with insufficient resources. Added a 300-mineral threshold (cost of Hatchery).
- [x] **R1.bonus3** `scripts/` and `wicked_zerg_challenger/scripts/` collided as namespace packages, hiding `ladder_tracker` / `meta_adapter`. Added `scripts/__init__.py` to make root a regular package.

### P1 Hot-path performance / correctness
- [ ] **R1.4** `economy_manager.py` does `from blackboard import …` inside on_step methods (5 sites). Hoist to module-level conditional import.
- [ ] **R1.5** `wicked_zerg_bot_pro_impl.py` has `E402 module level import not at top of file` (10+ sites). Move imports above runtime code.
- [ ] **R1.6** 223 broad `except Exception:` handlers, many silenced with `pass`. At minimum log at debug to enable post-mortem.

### P2 Code quality
- [ ] **R1.7** Many E501 (line too long) in `blackboard.py` and `wicked_zerg_bot_pro_impl.py`.
- [ ] **R1.8** `monitoring/mobile_app_android` Kotlin TODOs about hard-coded gist/pastebin URLs and missing file IO.

## Round 2 — Complete (1163 passed, 14 skipped, 0 failed)

### Real bugs fixed
- [x] **R2.1** `Dict[str, any]` → `Dict[str, Any]` in
  `economy/queen_transfusion_manager.py:233` and
  `advanced_micro_controller_v3.py:1405`. The lowercase `any` is the builtin
  function; the annotation was meaningless to type checkers.
- [x] **R2.2** Memory leak in `QueenTransfusionManager._queen_last_cast` —
  dead queen tags accumulated forever. Fixed by pruning entries against the
  live queen roster at the start of `execute_transfusions`.
- [x] **R2.3** Same memory-leak class in `QueenManager.last_transfuse_time` /
  `last_creep_time` / `assigned_queen_tags` / `dedicated_creep_queens`.
  Added `_prune_dead_queen_state()` invoked every 100 iterations from
  `on_step()`.
- [x] **R2.4** New regression tests:
  `test_dead_queen_cooldown_pruned`, `test_should_not_expand_supply_blocked`,
  `test_should_expand_at_exact_minerals_threshold`.

## Round 3 — Complete (1165 passed, 14 skipped, 0 failed)

### Real bugs fixed
- [x] **R3.1** `combat_manager.py:3483` defined `non_combat_names` set but
  never used it. The "scout-swarm = threat" logic was actually triggering on
  ANY 3 nearby enemies — including 3 of our own drones near the mineral
  line if they were filtered as enemies via mock, or 2 drones + 1 worker.
  Now it only fires on 3+ scout-class non-combat units.
- [x] **R3.2** `combat_manager.py:_find_harass_target` had two definitions —
  the first (line 2809) was dead code shadowed by the smarter version at
  ~5005. Removed the dead first copy (pyflakes F811) and added a comment
  to prevent re-introduction.
- [x] **R3.3** New regression tests
  `test_is_base_under_attack_only_drones_is_safe` and
  `test_is_base_under_attack_three_overlords_triggers` to lock in the
  scout-vs-worker distinction.

## Round 4+ — Planned
- Audit hot-path imports in `economy_manager.py` (5 lazy `from blackboard import` calls).
- E402 cleanup in `wicked_zerg_bot_pro_impl.py`.
- Inspect remaining F841 unused vars (`game_time` at 2717, `regenerating` x2 — likely intentional discards but worth double-checking).
- Tighten the 223 broad `except Exception:` handlers — at minimum add debug logging.
- Deeper static-analysis pass with `mypy` / `pyright`.
