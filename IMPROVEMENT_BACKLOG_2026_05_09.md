# SC2 Commander Bot — Improvement Backlog (2026-05-09)

> Generated by iterative test/inspection sweep on branch
> `claude/stoic-shannon-cJMyV`. Baseline: **372 pass / 34 skip / 1 collection
> error**, **690 ruff findings** across `wicked_zerg_challenger/` + `tests/`.

## Baseline summary

| Category | Count | Source |
|---|---|---|
| Tests passing | 372 | `pytest --ignore=tests/test_queen_transfusion.py` |
| Tests skipping | 34 | (env: sc2, numpy, pyupbit, config.yaml unavailable) |
| Collection errors | 1 | `tests/test_queen_transfusion.py` hard-imports `sc2` |
| Ruff F541 (f-string-missing-placeholders) | 255 | bot + tests |
| Ruff F841 (unused-variable) | 149 | bot + tests |
| Ruff F405 (undefined-local-with-import-star-usage) | 95 | `from ursina import *` consequences |
| Ruff E402 (module-import-not-at-top) | 72 | bot + tests |
| Ruff F401 (unused-import) | 49 | bot + tests |
| Ruff E712 (== True / == False) | 42 | mostly tests |
| Ruff E741 (ambiguous variable name) | 22 | bot |
| Ruff E713 (`not x in y` vs `x not in y`) | 2 | bot |
| Ruff F811 (redefined-while-unused) | 2 | **real bugs — silent method shadowing** |
| Ruff F823 (referenced-before-assignment) | 1 | **real bug — UnboundLocalError** |
| Ruff F403 (star-import) | 1 | visualization module |

## Round 1 — Real semantic bugs (highest priority)

| ID | File | Line | Issue |
|---|---|---|---|
| R1.1 | `wicked_zerg_challenger/wicked_zerg_bot_pro_impl.py` | 689 | Redundant `import traceback` inside `on_end()` shadows module-level import → `traceback.print_exc()` at L572 raises `UnboundLocalError` if reached. Remove inner import. |
| R1.2 | `wicked_zerg_challenger/opponent_modeling.py` | 765 | Second `on_step()` definition silently overrides the richer first definition (L341), losing build-order tracking, timing-attack detection, tech-progression tracking, blackboard updates, and update-throttling. Merge guards into L341, delete L765. |
| R1.3 | `wicked_zerg_challenger/local_training/production_resilience.py` | 1369 | Dead `build_terran_counters()` definition shadowed by L1866 (which uses TechCoordinator). Delete the dead L1369 version. |
| R1.4 | `tests/test_queen_transfusion.py` | 11 | Hard-imports `sc2.ids.unit_typeid` without skip-guard, breaking pytest collection on environments without `sc2`. Add same `try/except + pytest.skip(allow_module_level=True)` pattern used by sibling `test_queen_transfusion_manager.py`. |
| R1.5 | `wicked_zerg_challenger/comprehensive_unit_abilities.py` | 492 | `lambda l: not l.is_burrowed` — ambiguous single-letter name. Rename to `lurker`. |
| R1.6 | `wicked_zerg_challenger/base_destruction_coordinator.py` | 189 | `not u.type_id in worker_types` → `u.type_id not in worker_types`. |
| R1.7 | `wicked_zerg_challenger/combat/infestor_tactics.py` | 256 | `not unit_tag in self.infiltrating` → `unit_tag not in self.infiltrating`. |

## Round 2 — Safe ruff auto-fixes

- F541 f-strings without placeholders (255) → `ruff check --fix --select F541`
- F401 unused imports in tests (48) → `ruff check tests --fix --select F401`
- F841 unused variables (selected) where safe → `ruff check --fix --select F841 --unsafe-fixes` (review diff before commit)
- E713 remaining `not x in y` (2) → already in Round 1

## Round 3 — Test code quality

- E712 `== True` / `== False` in tests (42) → replace with truthy/falsy assertion or `is True`/`is False` where a strict bool is required
- E402 module-import-not-at-top in tests (8) → move imports to top
- E741 ambiguous variable names (22) → rename `l`, `I`, `O`

## Round 4+ — Larger refactors (deferred)

- F405 star-import side-effects in `wicked_zerg_challenger/visuals/swarm_3d_ursina.py` (95 hits from one `from ursina import *`) — explicit imports
- F841 unused-variable cleanup in bot package (149) — needs per-case review (some assignments are intentional for debugging)
- pytest skip/xfail labelling (34 skipped tests) — categorize env vs temporary-disabled
- Code-coverage threshold gate (`--cov-fail-under=70`)

## Process for each round

1. Apply fixes
2. Re-run `pytest --ignore=tests/test_queen_transfusion.py` (or unblock and run all)
3. Re-run `ruff check wicked_zerg_challenger/ tests/ --statistics`
4. Verify pass count did not drop
5. Commit with descriptive message, push
6. Update this file
