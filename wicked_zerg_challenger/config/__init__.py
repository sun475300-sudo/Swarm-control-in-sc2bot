"""Config sub-package for wicked_zerg_challenger.

Exists so that ``from config.unit_configs import …`` (used across
``wicked_zerg_challenger/*.py``) resolves the same way regardless of
whether the project root or the package directory is on ``sys.path``.
"""
