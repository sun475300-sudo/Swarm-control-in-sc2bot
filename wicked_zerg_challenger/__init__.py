"""Wicked Zerg Challenger – SC2 commander bot package.

Marker file so that ``wicked_zerg_challenger`` is importable as a regular
package on every Python (3.3+ implicit namespace packages can mask local
imports under some pytest layouts; pinning an explicit package avoids
that ambiguity).
"""
