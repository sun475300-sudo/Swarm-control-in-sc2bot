# 충돌 로직 정밀 검토 보고서 (Conflicting Logic Report)

사용자가 요청하신 "현재 충돌되는 로직"에 대해 코드베이스를 정밀 분석한 결과입니다. 시스템 안정성을 해치거나 의도치 않은 동작을 유발할 수 있는 3가지 주요 충돌을 발견했습니다.

## 1. 🚨 임포트 충돌 (Critical Crash)
*   **위치:** `run_with_training.py` (Line 628)
*   **코드:** `from tools.extract_and_train_from_training import TrainingDataExtractor`
*   **현상:** 해당 파일(`tools/extract_and_train_from_training.py`)이 실제 디렉토리에 존재하지 않습니다.
*   **결과:** 게임이 끝나고 학습 데이터 추출 단계로 진입하는 순간 **프로그램이 강제 종료**됩니다.
*   **해결:** 해당 라인을 주석 처리하거나 파일을 복구해야 합니다.

## 2. ⚔️ 자원 관리 충돌 (Resource Conflict)
*   **대상:** `ProductionResilience` (신규) vs `EconomyManager` (기존)
*   **분석:**
    *   `wicked_zerg_bot_pro_impl.py`에서 두 매니저가 모두 초기화됩니다.
    *   **충돌 포인트:** 두 매니저 모두 미네랄/가스 상태를 확인하고 일꾼 생산이나 건물 건설을 결정하려 할 수 있습니다. `ProductionResilience`는 "안전한 생산"을 목표로 하기에 기존 `EconomyManager`의 공격적인 확장 로직과 충돌하여 자원 부족(Starvation) 현상을 일으킬 수 있습니다.
*   **해결:** `ProductionResilience`를 메인 컨트롤러로 격상시키고, `EconomyManager`는 단순 자원 수집 역할만 하도록 역할을 명확히 분리해야 합니다.

## 3. 🤖 제어권 충돌 (Control Conflict)
*   **대상:** `RLAgent` (강화학습) vs `AggressiveStrategyExecutor` (하드코딩 전략)
*   **분석:**
    *   `RLAgent`는 신경망의 출력(Probability)에 따라 행동(공격/방어/확장 등)을 결정합니다.
    *   반면 `AggressiveStrategyExecutor`는 초반 12풀, 맹독충 올인 등 정해진 시나리오대로 유닛을 움직이려 합니다.
    *   **충돌 포인트:** RL이 "방어"를 선택했는데, 전략 모듈이 "전군 공격"을 명령하면 유닛들이 우왕좌왕(Oscillation)할 수 있습니다.
*   **해결:** 계층적 구조(Hierarchical)를 도입하여, 초반 5분은 `AggressiveStrategy`가 전권을 가지고, 그 이후에는 `RLAgent`가 지휘권을 넘겨받도록 조건을 명시해야 합니다.

---

## ✅ 종합 권고 사항

1.  **즉시 조치:** `run_with_training.py`의 618~687 라인(자동 추출 로직)을 `if False:`로 감싸서 비활성화하십시오. (충돌 방지 최우선)
2.  **구조 개선:** `BotStepIntegrator` 클래스 내부에서 매니저 간의 우선순위(Priority)를 엄격하게 정해야 합니다. (예: RL > Strategy > Basic Manager)
