# StarCraft 2 Bot Dependencies
# Core SC2 API library
# CRITICAL: Version conflicts resolved
# - burnysc2 7.x requires numpy>=2.0.0 (conflicts with our numpy<2.0.0)
# - burnysc2 6.x requires loguru<0.7.0 (conflicts with our loguru>=0.7.0)
# - burnysc2 5.0.12 requires numpy<=1.19.3 and loguru<0.6.0
# Solution: Use burnysc2 5.0.12 with relaxed numpy/loguru constraints
# Note: numpy 1.24.x works with Python 3.10 despite burnysc2's <=1.19.3 requirement
burnysc2==5.0.12

# Numerical operations
# CRITICAL: burnysc2 5.0.12 requires numpy<=1.19.3, but Python 3.10 needs >=1.20.0
# Using numpy 1.24.x as compromise (works in practice despite version mismatch)
numpy>=1.24.0,<2.0.0; python_version>="3.11"
numpy>=1.24.0,<2.0.0; python_version=="3.10"

# PyTorch for neural network features (optional but recommended)
# For GPU support: pip install torch>=2.0.0 --index-url https://download.pytorch.org/whl/cu118
torch>=2.0.0

# Enhanced logging (used by bot)
# CRITICAL: burnysc2 5.0.12 requires loguru<0.6.0, but we use features from >=0.6.0
# Using loguru 0.6.x as compromise (works in practice)
loguru>=0.6.0,<0.7.0

# Replay analysis for imitation learning
sc2reader>=1.8.0

# HTTP requests for API deployment
requests>=2.31.0

# Environment variable management (.env file support)
python-dotenv>=1.0.0

# Gemini API for self-healing system
# CRITICAL: Package name is google-generativeai, not google-genai
google-generativeai>=0.3.0

# Phone dashboard and Google Tasks integration (optional)
flask>=3.0.0
google-api-python-client>=2.0.0
google-auth-httplib2>=0.1.0
google-auth-oauthlib>=0.5.0

# Mobile dashboard backend
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
rich>=13.5.0

# CRITICAL: protobuf version constraint to prevent sc2 library conflicts
protobuf<=3.20.3

# WebSocket support for real-time dashboard
websocket-client>=1.6.0

# System and process utilities (for SC2 process monitoring)
psutil>=5.9.0
