# StarCraft 2 Bot Dependencies
# Core SC2 API library
burnysc2>=5.0.12

# Numerical operations
# CRITICAL: numpy<2.0.0 required for sc2 library compatibility
# Using pinned version for CI/CD stability
numpy>=1.20.0,<2.0.0; python_version<"3.12"
numpy>=1.24.0,<2.0.0; python_version>="3.12"

# PyTorch for neural network features (optional but recommended)
# For GPU support: pip install torch>=2.0.0 --index-url https://download.pytorch.org/whl/cu118
torch>=2.0.0

# Enhanced logging (used by bot)
loguru>=0.7.0

# Replay analysis for imitation learning
sc2reader>=1.8.0

# HTTP requests for API deployment
requests>=2.31.0

# Environment variable management (.env file support)
python-dotenv>=1.0.0

# Gemini API for self-healing system
# CRITICAL: Package name is google-generativeai, not google-genai
google-generativeai>=0.3.0

# Phone dashboard and Google Tasks integration (optional)
flask>=3.0.0
google-api-python-client>=2.0.0
google-auth-httplib2>=0.1.0
google-auth-oauthlib>=0.5.0

# Mobile dashboard backend
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
rich>=13.5.0

# CRITICAL: protobuf version constraint to prevent sc2 library conflicts
protobuf<=3.20.3

# WebSocket support for real-time dashboard
websocket-client>=1.6.0

