# 지휘관봇 개선 체크리스트 (테스트 기반 점검 결과)

> Generated by automated test/inspection sweep — 2026-05-15  
> 브랜치: `claude/stoic-shannon-K4jE3`  
> 테스트 환경: pytest 9.0.3 / Python 3.11.15 / 661 tests

## 0. 즉시 픽스 완료 (이 PR에서)
- [x] **F-001** `blackboard.py`: `Blackboard` 별칭 export 누락 → 2개 테스트 모듈 collection 실패
- [x] **F-002** `blackboard.py:should_expand`: 존재하지 않는 `is_supply_block` 속성 참조 → AttributeError
- [x] **F-003** `blackboard.py:should_expand`: minerals < 300이어도 True 반환 → Hatchery 비용 미체크
- [x] **F-004** `game_analytics_system.py`: 중복된 try/except 블록으로 SyntaxError (라인 419)
- [x] **F-005** `blackboard.py:auto_adjust_authority`: 지속되는 CRITICAL 위협에서 EMERGENCY↔COMBAT 30초 진동 버그 → sticky timeout-lock 추가
- [x] **F-006** `blackboard.py:request_production`: 0..3 범위 밖 priority 전달 시 KeyError → 최저(3)으로 demote
- [x] **F-007** `opponent_modeling.py:predict_strategy`: 두 곳의 ZeroDivisionError 가드 (corrupted load 시나리오)
- [x] **I-001** `FrameSkipManager` 봇에 와이어링 + blackboard 위협 상태 기반 combat-mode 자동 전환
- [x] **L-001** `production_controller.py` Blackboard 별칭 indirection 제거 (직접 import)
- [x] **L-002** `EnhancedScoutSystem` deprecation 경고 정리 (lazy import)
- [x] **T-001** `pytest-asyncio` deprecation 경고 정리
- [x] **Q-001a** `on_step`/`scoring_system.on_step`/`awareness_engine.on_step` 의 silent except를 throttled debug log로 교체
- [x] **CI-001** 전체 레포에 `black .` + `isort .` 적용 — CI lint 잡 main에서도 실패하던 것 해소

### 잘못 판정된 항목 (취소)
- ~~**D-001** `ai/behavior_tree.py:tick()`~~ → false positive: `@abstractmethod` 데코레이터 있음
- ~~**D-003** `monitoring/*.py`~~ → 의도된 placeholder 스텁
- ~~**I-002** `combat_manager._combat_frame_skip`~~ → 중복이 아닌 별도 레이어 (combat 미크로 내부 throttle)

## 1. 미사용 인프라 (구현되어 있지만 와이어링 안됨)
- [ ] **I-001** `utils/frame_skip.FrameSkipManager` — 클래스 정의·테스트 있지만 실제 봇 코드 어디에서도 import/사용 안 함
  - 효과: 매니저 호출 비용 절감, 전투 중 인텔/업그레이드 매니저 자동 스로틀
  - 위치: `wicked_zerg_bot_pro_impl.py` step 루프에 통합 필요
- [ ] **I-002** `combat_manager._combat_frame_skip` (자체 구현) — `FrameSkipManager`와 중복. 통합 또는 단일화 검토

## 2. 거대 함수 분해 (≥200 lines)
- [ ] **R-001** `bot_step_integration.py:812 execute_game_logic` — 1,726 lines (!!) — 모놀리식, 책임 분리 필요
- [ ] **R-002** `bot_step_integration.py:378 __init__` — 303 lines, 매니저 부팅 단계 분리
- [ ] **R-003** `bot_step_integration.py:2717 _safe_hierarchical_rl_step` — 253 lines
- [ ] **R-004** `combat_manager.py:406 _execute_multitasking` — 764 lines
- [ ] **R-005** `combat_manager.py:1813 _offensive_attack` — 231 lines
- [ ] **R-006** `economy_manager.py:2169 _check_proactive_expansion` — 234 lines

## 3. 조용한 예외 처리 (silent except: pass)
총 172건의 `except Exception: pass` 패턴 (로그 없음 + raise 없음)
- [ ] **Q-001** Top offenders 정리 후 최소 `logger.debug(...)` 추가
  - `realtime_awareness_engine.py` — 8건
  - `bot_step_integration.py` — 3건 (라인 2935, 3085, 3149)
  - `scoring_system.py` — 9건+
- [ ] **Q-002** 진짜로 무시해야 하는 케이스는 `# noqa` 또는 명시적 `contextlib.suppress`로 의도 표현

## 4. 매직 넘버
- [ ] **M-001** `blackboard.should_expand` 의 `HATCHERY_MINERAL_COST=300` — 상수 모듈로 추출
- [ ] **M-002** `frame_skip.DEFAULT_INTERVALS` — 매니저별 인터벌 설정파일로 이동 검토

## 5. 죽은/스텁 코드
- [ ] **D-001** `ai/behavior_tree.py:37 tick()` — docstring만 있음 (BT 노드 베이스 구현 누락 가능성)
- [ ] **D-002** `utils/extracted_utilities.py` — 13개 함수가 모두 docstring만, 사실상 placeholder
- [ ] **D-003** `monitoring/{bot_api_connector,config_server,remote_client}.py` — `__init__` pass만, 봇 모니터링 미구현
- [ ] **D-004** `local_training/advanced_building_manager.py:778` TODO — 워커 정지 감지 로직 누락

## 6. CLI/스크립트의 import-time side effects
- [ ] **S-001** `check_proxy.py` — import만 해도 OS-specific 경로 접근 시도 → 테스트/CI 환경에서 노이즈
- [ ] **S-002** `check_learning_rate.py` — import 시 로그 폭주
- [ ] **S-003** 패턴 통일: 스크립트는 `if __name__ == "__main__":` 가드 필수

## 7. Print → Logger 전환
- [ ] **P-001** 프로덕션 모듈에서 51건의 `print()` 호출 — `logger.info/debug` 로 통일

## 8. 추가 잠재 이슈 (후속 점검)
- [ ] **L-001** `production_controller.py:30` 의 `from blackboard import Blackboard as GameStateBlackboard` — 별칭 방향이 반대 (이제 이미 `Blackboard` export됨, 단순화 가능)
- [ ] **L-002** Deprecated `EnhancedScoutSystem` import in `bot_step_integration.py:158` — `AdvancedScoutingSystemV2`로 마이그레이션
- [ ] **L-003** 전역 변수 7건 (`opencl_accel`, `genai_self_healing`, `swarm_3d_ursina` 등) — 모듈 레벨 싱글톤 패턴이 스레드 안전한지 검증
- [ ] **L-004** `composition_optimizer`, `defense_coordinator` 등에서 expansion gate 테스트가 통과는 하지만 실제 의사결정 로직 회귀 가능성 — 통합 시나리오 테스트 추가

## 9. 테스트 인프라
- [ ] **T-001** `pytest_asyncio` deprecation: `asyncio_default_fixture_loop_scope` 미설정 → `pytest.ini`에 추가
- [ ] **T-002** 638 deprecation warnings (`google.protobuf` FileDescriptor) — 라이브러리 버전 호환 검토
- [ ] **T-003** `wicked_zerg_challenger/` 외부 (top-level) 에는 단위 테스트가 거의 없음 — 커버리지 격차

## 10. 작업 순서 우선순위 (이번 세션 진행)
1. ✅ F-001~F-004 (즉시 픽스, 완료)
2. → I-001 FrameSkipManager 와이어링 (이번 PR)
3. → Q-001 Silent except 톱 오펜더 정리
4. → L-002 Deprecated 스카우트 시스템 마이그레이션
5. → L-001 Blackboard 별칭 방향 단순화
6. → T-001 pytest_asyncio 경고 픽스
7. → R-001 ~ R-006 (별도 PR, 리팩토링은 신중히)
