[package]
name = "swarm_rust_accel"
version = "0.1.0"
edition = "2021"

[lib]
name = "swarm_rust_accel"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.22", features = ["extension-module"] }
